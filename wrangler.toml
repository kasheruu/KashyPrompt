# Deploy: npx wrangler deploy
# Secret: npx wrangler secret put GEMINI_API_KEY
name = "kashprompt"
main = "worker.js"
compatibility_date = "2024-09-01"

# Only ship the static UI — not repo root (would include node_modules / workerd.exe).
[assets]
directory = "./public"
binding = "ASSETS"
